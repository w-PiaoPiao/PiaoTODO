# 2026-06-30 开发日志

## 今日目标

- [x] Phase 1：搭建基础 UI 布局
- [x] Phase 2：定义数据类型和 Zustand store
- [x] Phase 3：通过分类按钮添加 todo
- [x] Phase 4：列表展示与分类筛选
- [x] Phase 5：完成、删除、编辑 todo
- [x] Phase 6：进度记录
- [x] Phase 7：已完成列表
- [x] Phase 8：拖拽排序
- [x] Phase 9：本地持久化
- [x] Phase 10：菜单栏集成与快捷键
- [x] Phase 11：设置与分类管理

## 完成事项

...

### Phase 5：完成、删除、编辑 todo
...

### Phase 6：进度记录
1. 创建 `src/components/ProgressDialog.tsx`。
2. TodoItem 集成 `useLongPress` 长按弹出进度对话框。
3. 进度历史时间线展示，最新进度显示在 todo 下方。

### Phase 7：已完成列表
1. 创建 `src/components/CompletedSection.tsx`：底部折叠区，显示已完成数量和清空按钮。
2. 已完成的 todo 按完成时间倒序排列。
3. 添加 `clearCompleted` action 并集成到 store。
4. 在 App.tsx 底部接入已完成区域。

### Phase 8：拖拽排序
1. TodoList 集成 `@dnd-kit`（DndContext + SortableContext），支持垂直拖拽排序。
2. TodoItem 添加拖拽手柄（≡ 图标），悬停时显示。
3. `PointerSensor` 配置 8px 激活距离，避免误触。
4. 排序结果通过 `reorderTodos` 更新到 store。
5. 拖拽时半透明反馈。

### Phase 9：本地持久化
1. Rust 端添加 `load_data` 和 `save_data` 命令，使用 `app_data_dir` 存取 JSON 文件。
2. 创建 `src/lib/storage.ts`：封装 Tauri invoke 调用。
3. todoStore 添加 `hydrate` action，启动时加载本地数据。
4. 集成 debounce 自动保存（300ms 防抖），每次状态变更自动写盘。
5. 首次启动时使用默认数据，后续每次打开自动恢复上一次状态。

### Phase 10：菜单栏集成与快捷键
1. 添加 Tauri tray-icon 功能，应用启动后在菜单栏显示图标。
2. 点击菜单栏图标切换主窗口的显示/隐藏。
3. 窗口定位在屏幕右上角（菜单栏下方），模拟原生菜单栏弹出效果。
4. 窗口失焦时自动隐藏（点击外部区域 → 面板收起）。
5. 注册全局快捷键 `Cmd + Shift + T` 切换面板。
6. 应用启动时窗口隐藏，仅显示菜单栏图标。

### Phase 11：设置与分类管理
1. 创建 `src/components/CategoryMenu.tsx`：长按分类按钮弹出管理菜单（只看此类 / 编辑名称 / 更改颜色 / 删除）。
2. 创建 `src/components/SettingsPanel.tsx`：完整的设置面板，可查看/添加/编辑/删除所有分类。
3. 编辑名称支持 inline input + Enter 保存；更改颜色提供 12 种预设色板。
4. 删除分类时自动保留至少 1 个分类。
5. 设置面板通过标题栏齿轮图标进入，`⬅` 返回。

## 验收检查

- [x] TypeScript 类型检查通过
- [x] Vite 构建成功
- [x] 单元测试全部通过（20 项）
- [x] 输入内容后点击分类按钮可添加 todo
- [x] 空输入时按钮抖动提示
- [x] 长按分类按钮弹出管理菜单
- [x] 可编辑分类名称和颜色
- [x] 可添加/删除分类
- [x] 设置面板可管理所有分类
- [x] 点击圆圈可完成/撤销完成
- [x] 单击文字可编辑
- [x] 悬停显示删除按钮
- [x] 长按 todo 可添加进度
- [x] 点击 todo 展开全部历史进度
- [x] 拖拽手柄可排序
- [x] 数据自动保存，重启后恢复
- [x] 菜单栏图标 + 点击切换窗口
- [x] `Cmd+Shift+T` 全局快捷键

## Notes

- 已完成 12 个 Phase（0-11），项目已用 Git 管理，每个 Phase 独立提交，已推送到 GitHub。
